factorial_recursive <- function(n) {
  if (n == 0) {
    return(1)
  } else {
    return(n * factorial_recursive(n - 1))
  }
}

# Test the function
num <- 5
print(paste("Factorial of", num, "is:", factorial_recursive(num)))
